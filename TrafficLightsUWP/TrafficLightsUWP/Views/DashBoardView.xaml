<Page
    x:Class="TrafficLightsUWP.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:TrafficLightsUWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TrafficLightsUWP"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding DashboardViewModel, Source={StaticResource ViewModelLocator}}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <controls:NavigationBar AddCommand="{Binding AddCommand}" EditCommand="{Binding EditCommand}" />
        <Button x:Name="button" Content="Button" HorizontalAlignment="Left" Margin="99,586,0,0" VerticalAlignment="Top" Width="459" Click="button_Click"/>
    </Grid>
</Page>
